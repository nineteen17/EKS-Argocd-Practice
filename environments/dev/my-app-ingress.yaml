apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app-ingress
  namespace: my-app
spec:
  rules:
    - host: myapp.xip.io # Use the subdomain you selected
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: my-custom-container
                port:
                  number: 80


